### How to run the experiments:

Build docker image from the provided Dockerfile and requirements.txt (docker must be running, and these commands must be executed from the project root directory):

```
docker build -t repl .
```

Run docker container:

```
docker run -it repl
```

Results are displayed in the console.
